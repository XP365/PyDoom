# This code sucks lmao

to connect to another user change the IP in NetworkManager to their IP. Only works on LAN. The first to connect starts hosting on their IP if the server doesnt have a connection.